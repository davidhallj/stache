## Open items in no specific order. 

1. Flatten RunStrategy sub-enums - all meaning has been hoisted to the top level 
enum. If a new RunStrategy flow is needed, a new top level enum should be created
2. Add support for other types of web proxy implementations (like Spring Rest)
3. Test @Stache flows / error messaging for if the field value is not properly set